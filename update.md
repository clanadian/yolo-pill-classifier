# 진행 상황 (2026-07-28)

## 완료
- 조명 조건에 따른 white_caplet ↔ pink_caplet 오분류 문제 확인 → 클래스별 색감 보정(LUT) 적용 (자세한 원인/과정은 `report.md` 4번 항목 참고)
- 웹캠으로 새 사진 촬영해서 추가: 흰 배경 120장(나) + 검은 배경 120장(팀원), 6클래스 전부 라벨링 완료
- **배경만 있는 네거티브 샘플 20장 추가** (검은 배경 15 / 흰 배경 5) — 라벨 `.txt`를 **의도적으로 빈 파일**로 둠. YOLO가 "이 사진엔 탐지할 대상이 없음"으로 학습하게 해서, 실제 데모에서 알약 없는 배경만 나올 때 엉뚱하게 오탐지하는 걸 줄이는 목적
- 데이터셋 전체 파일명을 클래스별 순번(000, 001, ...)으로 재정리, `split_and_build.py`로 `dataset/` 재생성
- 최종 클래스별 장수: capsule 149 / green_caplet 188 / mint_circle 169 / pink_caplet 186 / white_caplet 181 / yellow_caplet 187 / neg_black 15 / neg_white 5 (train 919 / val 161)
- `capsule_025`, `yellow_caplet_034` 라벨 오류(박스가 배경 위주로 잡힘) 발견해서 수동으로 좌표 재작성

## 알아둘 점 (팀원에게 전달 시 주의)
- **네거티브 샘플(`neg_black_*`, `neg_white_*`)의 빈 라벨은 버그가 아니라 의도된 것입니다.** 학습 전 데이터셋 검증 스크립트를 돌렸더니 "빈 라벨 20개 발견 → 데이터셋 문제로 학습 중단"으로 막혔는데, 걸린 파일 20개가 정확히 이 네거티브 샘플 전부였습니다. 검증 스크립트에서 파일명이 `neg_`로 시작하면 빈 라벨을 허용하도록 예외 처리 부탁드려요 (또는 빈 라벨 자체를 에러로 취급하지 않도록 완화 — ultralytics는 원래 빈 라벨을 정상 처리함).